export interface Product {
  id: string;
  name: string;
  price: number;
  category: string;
  image: string;
  rating: number;
  unit: string;
}

export interface User {
  id: string;
  name: string;
  email: string;
  phone: string;
}